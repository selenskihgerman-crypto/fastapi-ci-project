# 4. Опциональная программа для конвертации из ini в dict (упрощённо):

import configparser

config = configparser.ConfigParser()
config.read("logging_conf.ini")

for section in config.sections():
    print(f"[{section}]")
    for key, value in config.items(section):
        print(f"{key}: {value}")
